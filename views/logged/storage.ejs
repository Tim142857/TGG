<%- contentFor('header') %>
<%- contentFor('body') %>

<div class="row">
  <% for(var i = 0; i < user.buildings.length; i++){ %>
    <% if(user.buildings[i].type.functionBuilding.name === 'Stockage'){ %>
      <div class="col-lg-4 col-xs-12 text-center container-block-build">
        <div class="block-build">
          <div class='circle-level'>
            <i class="fa fa-circle fa-3x"></i>
            <span class="label-level"><%= user.buildings[i].level %></span>
          </div>
          <div class="container-title">
            <h3 class="h3"><%= user.buildings[i].type.name %></h3>
          </div>
          <div class="container-image">
            <img src="images/<%= user.buildings[i].type.image %>" class="img-fluid" />
          </div>
          <div>
            <span>Stockage actuel: <%= user.buildings[i].getValue() %></span><br/><br/>
            <span>Stockage au prochain niveau: <%= user.buildings[i].getValue() %></span><br/><br/>
            <form method="post" action="/build/levelUp">
              <input type="hidden" name="id" value="<%= user.buildings[i].id %>"></input>
              <button type="submit" class="btn btn-warning">
                <span>Améliorer pour <%= user.buildings[i].getCostNextLevel() %> <img src='images/<%= user.getStockageBuildByName('Or').type.ressource.image %>' alt='or' class='icon-ressource'> ?</span>
              </button>
            </form>
          </div>
        </div>
      </div>
      <% } %>
      <% } %>
    </div>


    <style>
      .container-block-build {
        padding: 20px;
      }
      .block-build {
        padding: 15px;
        border: 1px solid black;
        height: 440px;
      }

      .circle-level {
        position: absolute;
        top: 0px;
        right: 0px;
      }

      .label-level {
        padding-top: 5px;
        width: 40px;
        height: 40px;
        text-align: center;
        color: white;
        position: absolute;
        top: 3px;
        right: 0px;
      }

      .container-image {
        margin-bottom: 25px;
      }

    </style>

    <html>
      <div data-soldierName="Combattant" data-cost='45' class='.soldat'></div>
    </html>
    <script>
      function calcGold(var1, var2){
        //traitement
      }

      $(.soldat').on('change', function(){
        var name = $(this).data('soldierName')
        var cost = $(this).data('cost');
        calcGold(name, cost)
      })
    </script>
